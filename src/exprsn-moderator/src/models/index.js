/**
 * Moderator Service Models
 * Stub exports for models used by configuration routes
 */

// TODO: Implement Sequelize models based on database/schema.sql

const ContentFlag = {
  findAll: async () => [],
  count: async () => 0
};

const ModerationCase = {
  findAll: async () => [],
  count: async () => 0,
  findOne: async () => null
};

module.exports = {
  ContentFlag,
  ModerationCase
};
